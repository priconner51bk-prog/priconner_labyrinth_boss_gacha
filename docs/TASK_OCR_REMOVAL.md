# OCR操作経路撤廃タスク

## 現状（2026-09-13）

- 対象: `scripts/task_*.py` の実機タスク
- OCR参照: `scripts/task_*.py` では0件（2026-09-13）
- 操作タスクのOCR経路は撤廃し、テンプレート未登録の画面は安全停止する
- オフライン解析用のOCRライブラリ・設定・資料は残存しており、完全削除は未実施

## 完了条件

1. 実機操作スクリプトから `OCRServiceAdapter`、`PaddleOCRAdapter`、`.recognize()` の参照を除去する。
2. 固定UIはROIテンプレート判定へ移行する。
3. ボス名・キャラ候補など可変要素は登録済み画像／肖像テンプレート照合へ移行する。
4. テンプレート不足・一致失敗時は操作せず安全停止する。
5. OCR参照検索が0件になることを確認する。
6. 全テスト、compileall、clean clone検証を再実行する。
7. 実機のタイトルから入口までの安全遷移を再確認する。

## 実施順

1. 起動・ホーム・ギルド選択
2. ボス名・マップ・初期キャラ
3. 戦闘・報酬・ショップ・イベント
4. 残存参照の監査と全検証

## 状態

実機操作タスクのOCR撤廃は完了。マップ・難易度・一部敗北画面はテンプレート不足のため安全停止し、テンプレート取得後の再検証が必要。
